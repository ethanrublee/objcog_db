== Pre-Reqs ==
mongodb - 
  sudo apt-get install mongodb-dev
boost
cmake
== optional Pre-Reqs ==
opencv (a non rosinstall version)
pcl (a non rosinstall version)

== cmake build ==
mkdir build
cd build
cmake ..
make

== try the samples ==
build/bin/*

